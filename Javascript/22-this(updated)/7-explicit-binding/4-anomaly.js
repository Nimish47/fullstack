// Agenda: Do we really need call,apply and bind
// explain where obj.fn fails - chatgpt
// once section 6 is complete explain why happens behind the scenes - "call" binding happens
// explain behind the scenes what happens when - obj.fn() is called!
// The dot(.) at call time decides the value of "this"
// The dot only works when the function already lives on the object.
// call/apply exist for when that is NOT true.



// case:
// Problem here
// random fn and random obj
// binding using - call


// case:
// same as earlier
// solve using simple obj.fn
// Ques: why do we even need bind



