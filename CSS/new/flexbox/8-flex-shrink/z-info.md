## flex-shrink

## code-show
(show on chrome devtools)

1. play with different flex-shrink values
=> opposite of flex-grow
=> comes into action when we decrease container width down from (sum of all flex items width) i.e threshold
=> play with diff values
=> different flex-stems can shrink at diff speed


## observations
>> notice if if shrink enabled -> they won't go below content-size width, that's the true threshold