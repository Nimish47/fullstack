## element positioning in grid using grid-lines
    value starts from 1,2,3...
    to reference last grid-line, value = -1
    applied to grid element not grid container.

## grid-row: grid_line_start / grid_line_end
## grid-column: grid_line_start / grid_line_end


## VIDEO

1. explain the concept of grid-lines, row-lines and column-lines in a grid-container.
    - notice we have to first define grid column,rows widths and numbers in grid container.
    - then only we can use gridlines for each grid element.
    
2. this tutorial shows why grids are so powerful:
    entire page layout can be done via grid.
    much better than bootstrap.
3. each grid item is 'always' a rectangle.    