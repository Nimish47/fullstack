## grids
    very good for 3-d layouts

## grid-container and grid-elements
    obvious

## display: grid
    assigned to grid container    

## grid-template-columns
    specifies width of each column
    width of column  = width of grid element that occupies that space

## grid-template-rows
    specifies width of each row
    width of row  = width of grid element that occupies that space

## gap
    space b/w two grid elements (both row and column wise)
    we can also specify gap b/w rows and columns seperately (row-gap and column-gap)


## VIDEO
1. A very elementary grid container. How to define a container as a grid.
    display: grid (assigned to grid container)
2. why use grids?
3. grid-template-columns (px used for this lesson)
4. grid-template-rows (px used for this lesson)
5. explain what exactly a column and row mean in grid and that we give them a certain width?
6. gap, column-gap and row-gap 
7. based on 3 and 4, a grid is formed and each element (in grid container, like div here) falls into it.
8. explain: how element occupies its respective grid.
9. explain: what happens in case of a mismatch?
    2 grid columns and 2 grid rows but total grid elements =5
    where will the 5th grid element go?

